<h1> Adoptable Pups </h1> 

<% @adoptables.each do |pet| %>
    <h2><a href="/adoptables/<%= pet.pet_name %>"><%= pet.pet_name %></a> </h2>
    <img src="<%= pet.pet_pic %>" alt="puppy">
    <% end %>










